import { CqrsDispatcher } from "@/main/kernel/cqrs-dispatcher";

export function registerCodeAnalysisModule(cqrsDispatcher: CqrsDispatcher) {
  // CQRS handlers for code analysis would be registered here if they existed
}
