import { CqrsDispatcher } from "@/main/kernel/cqrs-dispatcher";

export function registerPersonaManagementModule(cqrsDispatcher: CqrsDispatcher) {
  // CQRS handlers for persona management would be registered here if they existed
}
