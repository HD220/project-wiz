import { CqrsDispatcher } from "@/main/kernel/cqrs-dispatcher";

export function registerUserSettingsModule(cqrsDispatcher: CqrsDispatcher) {
  // CQRS handlers for user settings would be registered here if they existed
}
