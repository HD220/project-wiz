export function findChannelsByProject(projectId: string): any[] {
  console.log("findChannelsByProject called with:", projectId);
  return [];
}

export function findAccessibleChannels(
  projectId: string,
  userId: string,
): any[] {
  console.log("findAccessibleChannels called with:", projectId, userId);
  return [];
}

export function createGeneralChannel(
  projectId: string,
  createdBy: string,
): any {
  console.log("createGeneralChannel called with:", projectId, createdBy);
  return {};
}
