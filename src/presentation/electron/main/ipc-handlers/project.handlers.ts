// Placeholder for project IPC handlers
// Content will be added in the next step
export function registerProjectHandlers() {
  // ipcMain.handle(IPC_CHANNELS.GET_PROJECTS_LIST, ...);
  // ipcMain.handle(IPC_CHANNELS.GET_PROJECT_DETAILS, ...);
  console.log('Project handlers (placeholder) registered.');
}
