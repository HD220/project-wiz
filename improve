#!/bin/bash

# Simple wrapper for auto-improvement-loop.sh
# Usage: ./improve

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "🎯 Project Wiz Auto-Improvement"
echo ""

# Run the improvement loop
exec "$SCRIPT_DIR/auto-improvement-loop.sh"